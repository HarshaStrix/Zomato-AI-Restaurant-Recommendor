[pytest]
testpaths = backend/tests
pythonpath = .
addopts = -v --tb=short
markers =
    integration: marks tests requiring network (deselect with '-m "not integration"')
    groq_integration: marks tests requiring GROQ_API_KEY (deselect with '-m "not groq_integration"')
